---
name: search-memory
description: 按关键词在记忆库中做子串检索（非向量），查找与当前任务相关的历史事实/事件/关系。
---

# search-memory（检索记忆）

在记忆 JSONL 文件中按**关键词子串**过滤匹配行（不建向量索引，适合本地轻量使用）。

## 何时使用

- 用户提到具体名词、项目名、错误号、日期片段等，需要在**全部记忆**中**定位**相关条目时。
- `browse_memory` 只看到最近若干条，若不够再使用 **`search_memory`** 扩大查找范围。

## 在 ReAct 中

- 直接调用工具 **`search_memory`**：
  - `query`：检索关键词或短语（必填）。
  - `max_per_kind`：每类最多返回条数（可选，默认 15）。

## 局限

- 基于子串匹配，同义词、语义相近但字面不同可能搜不到；需要时可换关键词或结合 `browse_memory`。

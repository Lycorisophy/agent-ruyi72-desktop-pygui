---
name: Word Writer
description: 创建Word文档：生成docx、格式化文本(字体/颜色/对齐)、段落格式、插入图片、创建表格、页面设置。
---
# Word Writer

创建Word文档：生成docx、格式化文本(字体/颜色/对齐)、段落格式、插入图片、创建表格、页面设置。

**触发词**: 创建 word 文档,写 word,生成 docx,word writer,new word document
